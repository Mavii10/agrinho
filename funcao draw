function draw() {
// Usando map () para ajustar a cor de fundo de forma mais controlada 
 let corfundo = IerpColor(217, 112,26), color(219,239,208),
 map (totalArvores,0,100,0, 1));
 background(corFundo);
mostrarInformacoes();
temperatura += 0.1;
jardineiro.atualizar();
jardineiro.mostrar();
